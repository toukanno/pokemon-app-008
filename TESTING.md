# テスト手順書 — モンスターズ・エレメンタ

## 1. 自動テスト

### 静的解析
```bash
flutter analyze
```
**期待結果**：`No issues found!`

### 単体テスト
```bash
flutter test
```
`test/game_logic_test.dart` に以下を収録（全13ケース）。`test/test_data_loader.dart` が
`assets/data/*.json` を直接読み込んで `GameData` を構築します。

| グループ | 検証内容 |
| --- | --- |
| データ整合性 | モンスター150体以上 / 進化先IDの実在 / 習得技IDの実在 / 相性表の全網羅 / 全マップが矩形 |
| 経験値テーブル | レベルと必要経験値の単調増加 / 累計経験値→レベルの逆算 |
| タイプ相性 | みず→ほのお=2倍、ほのお→みず=0.5倍 |
| バトルエンジン | ダメージ≥1 / 瀕死+状態異常で捕獲率上昇 |
| モンスター生成 | 生成個体は満タンHP・技1〜4個 / 進化で種族ID変更・整合性維持 |

**期待結果**：`All tests passed!`

### カバレッジ
```bash
flutter test --coverage
# coverage/lcov.info を生成
```

## 2. 手動テスト（受け入れ確認）

実機またはエミュレータで `flutter run` 後、以下を順に確認します。

### 2.1 起動・セーブ
- [ ] タイトルが表示される（セーブが無ければ「つづきから」非表示）
- [ ] 「さいしょから」→ なまえ入力・御三家3種から選択 → ワールド開始
- [ ] メニュー →「レポートをかく」でセーブ。アプリ再起動 →「つづきから」で復帰
- [ ] せってい → バックアップ書き出し → 削除 → 読み込みで復元できる

### 2.2 ワールド・移動
- [ ] 十字キー / 矢印キー / WASD で4方向移動。木・水・建物・岩で進入不可
- [ ] マップ端・ドアで隣エリア（街↔道路↔みなと↔どうくつ）へ移動
- [ ] NPC に向かって `A` で会話、看板を調べられる
- [ ] かいふくセンター（赤屋根）で全回復、ショップ（青屋根）で売買

### 2.3 エンカウント・バトル
- [ ] しげみでランダムエンカウントが発生
- [ ] 「たたかう」：技選択 → すばやさで先攻決定 → ダメージ・相性メッセージ表示
- [ ] 「こうかはばつぐん/いまひとつ」が属性に応じて出る
- [ ] 状態異常（どく・まひ・ねむり・やけど・こおり）が付与・解除・ターン経過する
- [ ] 「どうぐ」：キズぐすりでHP回復、状態回復薬が効く
- [ ] 「モンスター」：手持ち交代（瀕死時は強制交代）
- [ ] 「にげる」：野生戦から逃走できる
- [ ] 撃破で経験値獲得 → レベルアップ → 技習得 → 条件レベルで進化

### 2.4 捕獲・図鑑・ボックス
- [ ] エレメンボールで捕獲（HP低・状態異常で成功率上昇）
- [ ] 手持ち6匹超過分はボックスへ。ボックス↔手持ちの出し入れ
- [ ] ずかんに「みつけた/つかまえた」が反映。詳細で種族値・進化・習得技を表示

### 2.5 UI / 表示
- [ ] せってい → ダークモード切替が即時反映
- [ ] メッセージ速度（おそい/ふつう/はやい）が反映
- [ ] 端末を回転して縦・横どちらでも操作できる

## 3. データ再生成の検証
```bash
python3 tools/generate_data.py    # monsters/moves/types
python3 tools/generate_world.py   # world（全マップの矩形を assert で検証）
flutter test                      # 再生成後もテストが通ること
```

## 4. リグレッション基準

リリース前は最低限、以下がすべて成功していること。
1. `flutter analyze` → 0 issue
2. `flutter test` → 全 pass
3. 上記 2.x の手動チェックリスト完了
4. CI（`.github/workflows/build.yml`）の APK ビルド成功
